<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
  </head>

  <body>
    <p class="alert"><%= alert %></p>
    <p class="notice"><%= notice %></p>

    <% if person_signed_in? %>
      Logged in as <strong><%= current_person.email%><strong>
        <%= link_to 'Edit Profile', edit_person_registration_path, class: "btn btn-secondary btn-sm mx-5" %>|
        <%= link_to 'Logout', destroy_person_session_path, data: { turbo_method: :delete } , class: "btn btn-secondary btn-sm mx-5" %>
    <% else %>
        <%= link_to 'Sign up', new_person_registration_path, class: "btn btn-secondary btn-sm mx-5" %>|
        <%= link_to 'Login', new_person_session_path, class: "btn btn-secondary btn-sm mx-5" %>
    <% end %>

    <%= link_to "Admin Login", admin_root_path, class: "btn btn-secondary btn-sm mx-5 col-1 "%>
    <div class="m-5 " >
      <%= yield %>
    </div>
  </body>
</html>
